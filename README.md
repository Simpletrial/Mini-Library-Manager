# 📚 Mini Library Manager

Mini Library Manager is a Python-based project developed during my internship to manage a small collection of books. The project allows adding, updating, and retrieving book data stored in a JSON file, demonstrating Python fundamentals and file handling.

## 📁 Project Structure

mini-library-manager/
├── main.py
├── requirements.txt
├── README.md
├── library.json
├── .venv/

## ⚙️ Technologies Used

- Python 3  
- JSON file handling  

## 🚀 How to Run the Project

Clone the repository and navigate to the project folder:

git clone <your-github-repository-url>  
cd mini-library-manager  

Install the required dependencies:

pip install -r requirements.txt  

Run the project:

python main.py  

## 📝 Notes

- Book data is stored in `library.json`  
- Virtual environment files are excluded using `.gitignore`  
- This project was created as part of internship practice to strengthen Python fundamentals and project organization
